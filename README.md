# Neon Viper Web Terminal 
Static site that looks and feels like a bash terminal, because nerds like that kind of thing

## Setup

- Clone repo
- Run `npm install`
- Run `npm start`
- http://localhost:3000
